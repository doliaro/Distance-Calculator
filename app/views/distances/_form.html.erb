<%= form_for(@distance, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @distance.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@distance.errors.count, "error") %> prohibited this distance from being saved:</h4>

      <ul>
      <% @distance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :start_address, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :start_address, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :destination_address, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :destination_address, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
